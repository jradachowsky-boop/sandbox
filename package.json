{
  "name": "jeremy-strategy-os",
  "version": "1.0.0",
  "private": true,
  "description": "Executive operating system for conservation strategy leadership",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "rm -rf dist && mkdir -p dist/src && cp index.html dist/ && cp src/styles.css src/main.js dist/src/",
    "preview": "cd dist && python3 -m http.server 4173"
  }
}
